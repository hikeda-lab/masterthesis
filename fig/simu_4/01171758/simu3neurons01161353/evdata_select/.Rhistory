files <- list.files()
files[1]
pdf(file="test.pdf", height = 30,width= 30)
dev.off()
x <- 1:40
scan(file=files[1])
y<-scan(file=files[1])
plot(x,y,types="l")
plot(x,y,type="l")
pdf(file="test.pdf", height = 30,width= 30)
plot(x,y,type="l")
dev.off()
q()
library()
q()
